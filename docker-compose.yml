version: '3.8'
services:
  careerpath:
    build: 
      context: ./backend/careerpath
      dockerfile: dockerfile
    ports:
      - "3003:3003"
  frontend:
    build: 
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "80:3000"
    depends_on:
      - careerpath

